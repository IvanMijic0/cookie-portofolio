---
export const prerender = false;

const acceptLanguage = Astro.request.headers.get("accept-language")?.toLowerCase() ?? "";
const lang = /(^|,|\s)(bs|hr|sr|ba)(-|;|,|$)/.test(acceptLanguage) ? "ba" : "en";

return Astro.redirect(`/${lang}/homepage`);
---
